const AuthMiddleware = require("./auth");
const ChatMiddleware = require("./chat");
const PeerMiddleware = require("./peer");
const UserMiddleware = require("./user");

module.exports = {
    AuthMiddleware,
    ChatMiddleware,
    PeerMiddleware,
    UserMiddleware
}